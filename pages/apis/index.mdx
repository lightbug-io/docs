# API overview

The [version 1](v1-spec) API has been around since the early days of Lightbug.

The [version 2](v2-spec) API is being rolled out starting in 2023.

Both APIs are accessible on the host `api.lightbug.cloud`.
