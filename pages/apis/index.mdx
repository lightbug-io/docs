# API overview

The [version 1](apis/v1) API has been around since the early days of Lightbug.

The [version 2](apis/v2) API is being rolled out starting in 2023, but is NOT currently declared stable.

Both APIs are accessible on the host `api.lightbug.cloud`.
