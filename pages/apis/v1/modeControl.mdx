import { Callout } from 'nextra/components'
import ModeControl from '../../../components/helper-tools/modecontrol'

# Mode Controls

The mode controls are any boolean (true/false) flags that control device behaviour.

These are currently exposed across 2 device settings in the version 1 API.

### modeControl

<Callout emoji="🧮">
  Use this calculator to determine the correct value to use for the `modeControl` settings.
  <ModeControl />
</Callout>

### modeControl2

<Callout emoji="🧮">
  Use this calculator to determine the correct value to use for the `modeControl2` settings.

  TODO calculator
</Callout>
